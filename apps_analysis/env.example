# 环境变量配置示例
# 复制此文件为 .env 并修改相应配置

# 配置文件路径
CONFIG_PATH=config.yaml

# 服务器配置
SERVER_HOST=0.0.0.0
SERVER_PORT=8000

# LLM API 配置（如果需要在环境变量中配置）
LLM_BASE_URL=http://10.163.25.156:5024/v1
LLM_API_KEY=zhipu2025ddh
LLM_MODEL_NAME=external-ds-v3_2

# MinIO 配置（可选）
MINIO_ENDPOINT=localhost:9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_BUCKET=logllm
MINIO_SECURE=false

# 数据库配置（可选）
DATABASE_TYPE=sqlite
DATABASE_PATH=data/analyzer.db
